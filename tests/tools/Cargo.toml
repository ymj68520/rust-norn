[package]
name = "tx-generator"
version = "0.1.0"
edition = "2021"

# 独立工作空间（不继承父workspace）
[workspace]

[dependencies]
# 依赖父项目crate
norn-crypto = { path = "../../crates/crypto" }
norn-common = { path = "../../crates/common" }

# 外部依赖
hex = "0.4"
chrono = "0.4"
anyhow = "1.0"
rand = "0.8"
